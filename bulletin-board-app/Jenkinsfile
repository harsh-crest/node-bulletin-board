pipeline {
    agent any
    stages {
		stage('Checkout') {
			checkout scm
    }
        stage('Example Build') {
            steps {
                sh 'echo "harsh"'
                sh 'ls -l'
            }
        }
    }
}
